bug：“我的网盘”页面中的复制下载链接功能，正常情况下是可以的，但如果点击按名称/大小/修改时间改顺序后，长按下载按钮后就还是原来顺序的文件，而不是新顺序的文件（比如a文件默认排序在第一个，我点击第一行的下载按钮就是复制它的链接，但改顺序后b文件排在了第一个，点击同个按钮仍然是a的下载链接）
修改功能：我想让网盘的文件位置导航栏按钮、顶部/侧面导航栏按钮实现点击时按住ctrl可以在新页面中打开它（就像`<a>`链接一样），现在我的实现方式是新开一个页面为当前链接，然后当前页面进行跳转
修改功能：我设置了一个参数`pureEdition`来控制页面背景图片的样式，我想让默认`pureEdition=True`
新增功能：在“我的网盘”页面的预览界面中，可以修改文本类型的文件，比如在预览界面加一个修改按钮，点击后切换成文本框，点击完成按钮提交新文件（只有对该文件有操作权限时显示）；对于文本类型的文件，在预览界面加一个“复制全文”的按钮
新增功能：在网盘界面中，文件夹的大小现在是不显示的，能不能把文件夹的大小也显示（改一下后端的接口）。之后在选择文件时在右上角“已选择xxx个文件”区域一并显示已选择文件的总大小，并设置下载多个文件的大小限制为1G（单个文件不限制）（仅前端作校验即可）
bug：在点击图片预览时，滚动条会消失从而导致页面右移，有办法解决吗？如果是因为ele组件不好更改也可以不改
新增功能：在网盘界面中，如果当前路径不存在/访问权限不够自动返回上一级
新增功能：如果我设置背景图片轮换，那么现在的情况是切到哪张图片才加载哪张图片，能不能让背景图片提前加载，并检测加载状态，哪张加载好了就加载哪张
一个疑问：我发现现在在上传文件时，最多只有6个文件能同时上传，这个“6”是linux服务器的限制吗？

新增功能（比较复杂，最后做）：新增注册用户页面（并修改对应后端数据库和接口），只有admin用户能注册/修改，可以注册admin/user级别的用户。同时提供修改密码的功能（只要是登录状态即可修改，只能改自己的密码）
新增功能（比较复杂，最后做）：在文章界面中，加一个背景音乐(bgm)的功能，直接加在“文章信息”的表中即可（前端的更新文章信息和新增文章表单也同步更新），我会把音乐像背景图片一样直接放进uploads文件夹中（网盘中），也是直接通过下载链接即可下载。在文章界面的边角处加一个音乐控制器（暂停、播放、切换歌），可以做成一个按钮 鼠标移入/点击后展开的那种，进入页面时用ele的侧面message提示该音乐控制器的存在，默认不播放
